import * as Enzyme from 'enzyme';
import * as React from 'react';
import { Edit#resource# } from '../../src/#pluralLower#/Edit#resource#';

describe('Edit#resource#', (): void => {
  xit('does something', (): void => {
    const component = Enzyme.shallow(<Edit#resource# />);
  });
});
