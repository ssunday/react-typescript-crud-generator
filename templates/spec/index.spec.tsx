import * as Enzyme from 'enzyme';
import * as React from 'react';
import { Index#resource# } from '../../src/#pluralLower#/Index#resource#';

describe('Index#resource#', (): void => {
  xit('does something', (): void => {
    const component = Enzyme.shallow(<Index#resource# />);
  });
});
