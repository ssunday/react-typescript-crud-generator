import * as Enzyme from 'enzyme';
import * as React from 'react';
import { Show#resource# } from '../../src/#pluralLower#/Show#resource#';

describe('Show#resource#', (): void => {
  xit('does something', (): void => {
    const component = Enzyme.shallow(<Show#resource# />);
  });
});
